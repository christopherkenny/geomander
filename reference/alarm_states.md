# List Available States from ALARM Data

List Available States from ALARM Data

## Usage

``` r
alarm_states()
```

## Value

character vector of state abbreviations inferred from the ALARM
repository contents.

## Examples

``` r
if (FALSE) { # \dontrun{
# relies on internet availability and interactivity on some systems
alarm_states()
} # }
```
