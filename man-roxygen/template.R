#' <%=ifelse(exists("epsg") && epsg, "@param epsg numeric EPSG code to planarize to. Default is 3857.", "") %>
#' <%=ifelse(exists("tolerance") && tolerance, "@param tolerance poistive numeric tolerance to simplify by. Default is 0.01.", "") %>
